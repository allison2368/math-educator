OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
# Serverless (Lambda/Vercel): use bundled embeddings, no Chroma writes
# RAG_BACKEND=memory
# Optional writable Chroma path (defaults to /tmp/chroma_db on Lambda)
# CHROMA_PATH=/tmp/chroma_db
